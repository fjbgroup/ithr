["'; }\n\nvar _editData     = @json($items->keyBy('", "'));\nvar _brandOpts    = @json($brands->pluck('", "'));\nvar _locationOpts = @json($locations->pluck('", "'));\n\n\nfunction openEditModal(id) {\n  var d = _editData[id];\n  if (!d) return;\n  var isAdminOrFinance = {{ $user->isAdminOrFinance() ? '", "' }};\n\n  document.getElementById('", "' + id;\n  \n  // New fields\n  document.getElementById('", "';\n  document.getElementById('", "';\n  document.getElementById('", "';\n  document.getElementById('", "';\n  \n  document.getElementById('", "';\n  document.getElementById('", "';\n  document.getElementById('", "';\n  document.getElementById('", "';\n  document.getElementById('", "';\n  document.getElementById('", "';\n  document.getElementById('", "';\n  document.getElementById('", "';\n  document.getElementById('", "';\n  document.getElementById('", "';\n  document.getElementById('", '\';\n  \n  // Date fields need to be in YYYY-MM-DD if input is type="date", we might need to parse. Assuming d.purchase_date is string or we can just try assigning. \n  // Wait, if it\'', "'s just assign substring(0,10)\n  document.getElementById('", "';\n  document.getElementById('", "';\n  \n  document.getElementById('", "';\n  document.getElementById('", "';\n  document.getElementById('", "';\n  document.getElementById('", "';\n  document.getElementById('", "';\n  document.getElementById('", "';\n  document.getElementById('", "';\n  document.getElementById('", "';\n  document.getElementById('", "';\n  document.getElementById('", "';\n  document.getElementById('", "';\n  document.getElementById('", "';\n  document.getElementById('", "';\n  document.getElementById('", "';\n  document.getElementById('", "';\n\n  if (!isAdminOrFinance) {\n    document.getElementById('", "';\n    document.getElementById('", "';\n    document.getElementById('", "';\n    document.getElementById('", "';\n  } else {\n    document.getElementById('", "';\n    document.getElementById('", "';\n    document.getElementById('", "';\n    document.getElementById('", "';\n  }\n\n  if (d.updated_at) {\n    var dt = new Date(d.updated_at);\n    document.getElementById('", "').innerHTML =\n      '", "'});\n  } else {\n    document.getElementById('", "';\n  }\n\n  document.getElementById('", "';\n  document.getElementById('", "';\n  document.getElementById('", "'});\n}\nfunction closeItEditForm() {\n  document.getElementById('", "';\n  document.getElementById('", "';\n}\nfunction selectOpt(el, gridId, hiddenId, val) {\n  document.querySelectorAll('", "').forEach(function(o) {\n    o.classList.remove('", "');\n    o.style.borderColor = '", "';\n    o.style.background  = '", "';\n    o.style.color       = '", "';\n    var ic = o.querySelector('", "';\n  });\n  var clr = el.dataset.color || '", "';\n  var bg  = el.dataset.bg    || '", "';\n  el.classList.add('", "');\n  el.style.borderColor = clr;\n  el.style.background  = bg;\n  el.style.color       = clr;\n  var ic = el.querySelector('", "'); if (ic) ic.style.color = clr;\n  document.getElementById(hiddenId).value = val;\n}\nfunction nitSetGrid(gridId, hiddenId, val) {\n  document.querySelectorAll('", "').forEach(function(o) {\n    o.classList.remove('", "');\n    o.style.borderColor = o.style.background = o.style.color = '", "';\n    var ic = o.querySelector('", "';\n  });\n  document.querySelectorAll('", "').forEach(function(o) {\n    var onclick = o.getAttribute('", "';\n    var match = onclick.match(/'", "'\\s*\\)$/);\n    if (match && match[1] === val) {\n      var clr = o.dataset.color || '", "';\n      var bg  = o.dataset.bg    || '", "';\n      o.classList.add('", "');\n      o.style.borderColor = clr; o.style.background = bg; o.style.color = clr;\n      var ic = o.querySelector('", "'); if (ic) ic.style.color = clr;\n    }\n  });\n  document.getElementById(hiddenId).value = val;\n}\n\n// ── PERSISTENT CROSS-PAGE SELECTION ──\nconst _SEL_KEY = '", "';\nconst selectedIds = new Set(JSON.parse(sessionStorage.getItem(_SEL_KEY) || '", "'));\nfunction _persistSel() { sessionStorage.setItem(_SEL_KEY, JSON.stringify([...selectedIds])); }\n\nfunction syncCheckboxes() {\n  const rows = document.querySelectorAll('", "');\n  const selectAll = document.querySelector('", "');\n  if (!selectAll) return;\n  let checkedCount = 0;\n  rows.forEach(cb => {\n    cb.checked = selectedIds.has(cb.value);\n    if (cb.checked) checkedCount++;\n  });\n  const total = rows.length;\n  selectAll.checked       = total > 0 && checkedCount === total;\n  selectAll.indeterminate = checkedCount > 0 && checkedCount < total;\n}\n\ndocument.addEventListener('", "', function(e) {\n  if (e.target.classList.contains('", "') && !e.target.disabled) {\n    if (e.target.checked) selectedIds.add(e.target.value);\n    else selectedIds.delete(e.target.value);\n    _persistSel();\n    syncCheckboxes();\n    updateBulkBar();\n  }\n});\n\ndocument.addEventListener('", "', function(e) {\n  if (e.target.id === '", "') {\n    document.querySelectorAll('", "').forEach(cb => {\n      cb.checked = e.target.checked;\n      if (e.target.checked) selectedIds.add(cb.value);\n      else selectedIds.delete(cb.value);\n    });\n    _persistSel();\n    updateBulkBar();\n  }\n});\n\nwindow._onDtDraw = function() {\n  syncCheckboxes();\n  updateBulkBar();\n};\n\nfunction clearSelection() {\n  selectedIds.clear();\n  _persistSel();\n  const selectAll = document.querySelector('", "');\n  if (selectAll) { selectAll.checked = false; selectAll.indeterminate = false; }\n  document.querySelectorAll('", "').forEach(cb => cb.checked = false);\n  updateBulkBar();\n}\n\nfunction goToWriteoff() {\n  if (!selectedIds.size) return;\n  window.location.href = '", "');\n}\n\nfunction submitBulkDelete() {\n  if (!selectedIds.size) return;\n  if (!confirm('", "')) return;\n  const form = document.getElementById('", "');\n  const container = document.getElementById('", "');\n  container.innerHTML = '", "';\n  selectedIds.forEach(id => {\n    const inp = document.createElement('", "');\n    inp.type = '", "'; inp.value = id;\n    container.appendChild(inp);\n  });\n  selectedIds.clear();\n  _persistSel();\n  form.submit();\n}\n\nfunction updateBulkBar() {\n  const bar   = document.getElementById('", "');\n  if (!bar) return;\n  const count = selectedIds.size;\n  document.getElementById('", "').textContent = count;\n  bar.style.display = count > 0 ? '", "';\n}\n\nfunction startImport() {\n  var file = document.getElementById('", "').files[0];\n  if (!file) { alert('", "'); return; }\n  var reader = new FileReader();\n  reader.onload = function(e) {\n    var wb = XLSX.read(e.target.result, { type: '", "', cellDates: true });\n    var ws = wb.Sheets[wb.SheetNames[0]];\n    var rows = XLSX.utils.sheet_to_json(ws, { defval: '", "' });\n    if (!rows.length) { alert('", "'); return; }\n    var result = document.getElementById('", "');\n    result.style.display = '", "';\n    result.innerHTML = '", "';\n    fetch('", "', {\n      method: '", "',\n      headers: { '", "').content },\n      body: JSON.stringify(rows)\n    }).then(function(r){ return r.json(); }).then(function(d) {\n      var color = d.inserted > 0 ? '", "';\n      var bg = d.inserted > 0 ? '", "';\n      var border = d.inserted > 0 ? '", "';\n      result.innerHTML = '", "'\n        + '", "'\n        + (d.errors && d.errors.length ? '", "')\n        + '", "';\n      if (d.inserted > 0) setTimeout(function(){ location.reload(); }, 1500);\n    }).catch(function(){ result.innerHTML = '", "'; });\n  };\n  reader.readAsArrayBuffer(file);\n}\n\n// ── QR CODE MODAL ──\nvar _qrInstance = null;\nvar _qrCurrentId = null;\nvar _qrAssetData = {};\n\nfunction openQRModal(id, assetNo, desc, cls, serial, brand, model, location) {\n  _qrAssetData = { id: id, assetNo: assetNo, desc: desc, cls: cls };\n  _qrCurrentId = id;\n\n  var sep = '", "';\n  var na  = function(v) { return (v && String(v).trim()) ? String(v).trim().toUpperCase() : '", "'; };\n\n  // Plain text payload — iOS camera shows this as a TEXT card, not a browser link\n  var text = '", "'\n    + sep + '", "'\n    + '", "'\n'", "'\n'", "'\n'", "'\n'", "'\n'", "'\n'", "'\n'", "'\n'", "';\n\n  document.getElementById('", "' + id);\n  document.getElementById('", "';\n  document.getElementById('", "';\n\n  var container = document.getElementById('", "');\n  container.innerHTML = '", "';\n  _qrInstance = new QRCode(container, {\n    text: text, width: 180, height: 180,\n    colorDark: '", "',\n    correctLevel: QRCode.CorrectLevel.M\n  });\n\n  document.getElementById('", "';\n}\n\nfunction printQR() {\n  var canvas = document.querySelector('", "');\n  if (!canvas) return;\n  var w = window.open('", "');\n  w.document.write('", "');\n  w.document.write('", "');\n  w.document.write('", "');\n  w.document.write('", "');\n  w.document.write('", "');\n  w.document.close(); w.focus();\n  setTimeout(function() { w.print(); w.close(); }, 300);\n}\n\nfunction downloadQR() {\n  var canvas = document.querySelector('", "');\n  if (!canvas) return;\n  var link = document.createElement('", "');\n  link.download = '", "';\n  link.href = canvas.toDataURL();\n  link.click();\n}\n\ndocument.getElementById('", "', function(e) {\n  if (e.target === this) this.style.display = '", "';\n});\n\n// ── AUTOCOMPLETE HELPER ──\nfunction makeAutocomplete(inputId, boxId, url, minLen) {\n  var inp = document.getElementById(inputId);\n  var box = document.getElementById(boxId);\n  if (!inp || !box) return;\n  var timer;\n\n  inp.addEventListener('", "', function() {\n    clearTimeout(timer);\n    var q = this.value.trim();\n    if (q.length < minLen) { box.style.display = '", "'; return; }\n    timer = setTimeout(function() {\n      fetch(url + '", "' + encodeURIComponent(q))\n        .then(function(r) { return r.json(); })\n        .then(function(items) {\n          if (!items.length) { box.style.display = '", "'; return; }\n          box.innerHTML = items.map(function(d) {\n            return '", "';\n          }).join('", "');\n          var last = box.querySelector('", "');\n          if (last) last.style.borderBottom = '", "';\n          box.style.display = '", "';\n        })\n        .catch(function() { box.style.display = '", "'; });\n    }, 220);\n  });\n\n  box.addEventListener('", "', function(e) {\n    var item = e.target.closest('", "');\n    if (item) { inp.value = item.textContent; box.style.display = '", "'; }\n  });\n  box.addEventListener('", "', function(e) {\n    var item = e.target.closest('", "');\n    if (item) item.style.background = '", "';\n  });\n  box.addEventListener('", "', function(e) {\n    var item = e.target.closest('", "');\n    if (item) item.style.background = '", "';\n  });\n  document.addEventListener('", "', function(e) {\n    if (!inp.contains(e.target) && !box.contains(e.target)) box.style.display = '", "';\n  });\n}\n\nmakeAutocomplete('", "', 2);\nmakeAutocomplete('", "',        1);\nmakeAutocomplete('", "',        1);\n\n// ── MAIN SEARCH AUTOCOMPLETE ──\n(function() {\n  var inp = document.getElementById('", "');\n  var box = document.getElementById('", "');\n  if (!inp || !box) return;\n  var timer, active = -1;\n\n  inp.addEventListener('", "', function() {\n    clearTimeout(timer);\n    active = -1;\n    var q = this.value.trim();\n    if (q.length < 2) { box.style.display = '", "'; return; }\n    timer = setTimeout(function() {\n      fetch('", "' + encodeURIComponent(q))\n        .then(function(r) { return r.json(); })\n        .then(function(items) {\n          if (!items.length) { box.style.display = '", "'; return; }\n          box.innerHTML = items.map(function(item) {\n            return '", "'\n              + '", "'\n              + String(item.label).replace(/</g, '", "';\n          }).join('", "');\n          var last = box.querySelector('", "');\n          if (last) last.style.borderBottom = '", "';\n          box.style.display = '", "';\n          box.querySelectorAll('", "').forEach(function(el) {\n            el.addEventListener('", "')).indexOf(el)); });\n            el.addEventListener('", "', function() { selectItem(el); });\n          });\n        })\n        .catch(function() { box.style.display = '", "'; });\n    }, 220);\n  });\n\n  inp.addEventListener('", "', function(e) {\n    var items = box.querySelectorAll('", "');\n    if (!items.length) return;\n    if (e.key === '", "') { e.preventDefault(); setActive(Math.min(active + 1, items.length - 1)); }\n    else if (e.key === '", "') { e.preventDefault(); setActive(Math.max(active - 1, -1)); }\n    else if (e.key === '", "' && active >= 0) { e.preventDefault(); selectItem(items[active]); }\n    else if (e.key === '", "'; active = -1; }\n  });\n\n  document.addEventListener('", "', function(e) {\n    if (!inp.contains(e.target) && !box.contains(e.target)) { box.style.display = '", "'; active = -1; }\n  });\n\n  function setActive(idx) {\n    var items = box.querySelectorAll('", "');\n    items.forEach(function(el, i) { el.style.background = i === idx ? '", "'; });\n    active = idx;\n  }\n\n  function selectItem(el) {\n    inp.value = decodeURIComponent(el.dataset.value);\n    box.style.display = '", "';\n    active = -1;\n    document.getElementById('", "').submit();\n  }\n})();\n\n// Live search\n(function () {\n  var input   = document.getElementById('", "');\n  var results = document.getElementById('", "');\n  var form    = document.getElementById('", "');\n  if (!input || !results || !form) return;\n  var timer;\n  input.addEventListener('", "', function () {\n    clearTimeout(timer);\n    timer = setTimeout(function () {\n      var params = new URLSearchParams();\n      params.set('", "', input.value.trim());\n      form.querySelectorAll('", "').forEach(function (sel) {\n        if (sel.value) params.set(sel.name, sel.value);\n      });\n      params.set('", "');\n      results.style.opacity = '", "';\n      fetch('", "' + params.toString())\n        .then(function (r) { return r.text(); })\n        .then(function (html) { results.innerHTML = html; results.style.opacity = '", "'; })\n        .catch(function () { results.style.opacity = '"]